namespace Jobtech.OpenPlatforms.GigPlatformApi.PlatformEngine.Managers
{
    public interface IConnectionManager
    {
        //Task<Connection> GetConnectionAsync(ConnectionId id);
        //Task<IEnumerable<Connection>> GetConnectionsAsync();
        //Task<bool> ConnectionExistsAsync(string id);
        //Task<bool> ConnectionExistsAsync(User user, Platform platform);
        //Task RemoveConnectionAsync(ConnectionId id);
        //Task<UserDataRequest> GetRequestTokenAsync(Connection connection);
        //Task UpdateUserDataAsync(ConnectionId connectionId);
        //Task<bool> ConnectionExistsAsync(PlatformId platformId, UserId userId);
        //Task<Connection> SynchronizeConnectionAsync(Platform platform, User user);
        //Task<Connection> SynchronizeConnectionAsync(ConnectionId connectionId);
        //Task<Connection> EstablishConnectionAsync(Platform platform, User user);
        //Task<IEnumerable<Connection>> GetConnectionsAsync(UserId userId);
    }
}